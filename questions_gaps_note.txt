Questions

1. Should the .env file be mutual, located at the highest level of the file tree? (Currently, each service has its own .env file).
2. Should move Redis to a cloud service?
3. Should deploy with Docker?